name: test for Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/base.yml

    - name: Test
      run: go test -v ./...
